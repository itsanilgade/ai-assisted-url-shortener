# Security Notes

Do not report or store real customer data, credentials, access tokens, or secrets in this repository or AI prompts. Production deployments must add authentication/authorization to management endpoints, use a managed secret store, terminate TLS, define analytics retention, and place abuse controls at the edge.
